# Project Health Intelligence Mock

Standalone frontend-only product vision mock. It uses local mock data and does not connect to People Portal, Authentik, Gitea, MongoDB, or any other service.

## Run locally

```bash
npm run dev
```

Then open <http://localhost:4173>.

The mock is intentionally dependency-free so it can be shared quickly during product conversations.
